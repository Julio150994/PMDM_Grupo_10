export interface Usuario {
    firstname: string;
    secondname: string;
    email: string;
    password: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    c_password: boolean;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    company_id: number;
}

